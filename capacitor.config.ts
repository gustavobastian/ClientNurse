import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Gabiot.enfermera',
  appName: 'SistemaEnfermera',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
